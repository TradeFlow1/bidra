export const dynamic = "force-dynamic";
export const revalidate = 0;

import "./globals.css";
import Link from "next/link";

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <header style={{ borderBottom: "1px solid #d1d5db" }}>
          <div
            style={{
              maxWidth: 1100,
              margin: "0 auto",
              padding: "12px 16px",
              display: "flex",
              alignItems: "center",
              justifyContent: "space-between",
              gap: 16,
            }}
          >
            <Link href="/" style={{ display: "flex", alignItems: "center", gap: 12, textDecoration: "none" }}>
              <img
                src="/brand/bidra-logo_light.png"
                alt="Bidra"
                width="220" height="56"
                style={{ display: "block", filter: "drop-shadow(0 2px 6px rgba(0,0,0,0.25))", transform: "translateY(-1px)" }}
              />
            </Link>

            <nav style={{ display: "flex", gap: 16, alignItems: "center", flexWrap: "wrap" }}>
              <Link href="/listings">Browse</Link>
              <Link href="/sell">Sell</Link>
              <Link href="/dashboard">Dashboard</Link>
              
              <Link href="/profile">Account</Link>
<Link href="/legal">Legal</Link>
              <Link href="/help">Help</Link>
              <Link href="/signin">Sign in</Link>
              <Link
                href="/register"
                style={{
                  padding: "8px 12px",
                  background: "#2563eb",
                  color: "white",
                  borderRadius: 8,
                  textDecoration: "none",
                  fontWeight: 700,
                }}
              >
                Get started
              </Link>
            </nav>
          </div>
        </header>

        <main style={{ maxWidth: 1100, margin: "0 auto", padding: "16px" }}>
          {children}
        </main>
      </body>
    </html>
  );
}